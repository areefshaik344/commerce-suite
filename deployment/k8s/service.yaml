apiVersion: v1
kind: Service
metadata:
  name: commerce-suite
  namespace: commerce-suite
spec:
  type: ClusterIP
  selector: { app: commerce-suite }
  ports:
    - name: http
      port: 80
      targetPort: 8080
