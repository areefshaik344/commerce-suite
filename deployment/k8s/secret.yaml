# TEMPLATE ONLY — actual secrets must come from Vault / AWS SM / GCP SM / Azure KV
apiVersion: v1
kind: Secret
metadata:
  name: commerce-suite-secrets
  namespace: commerce-suite
type: Opaque
stringData:
  DB_URL: "jdbc:postgresql://postgres:5432/commerce_suite"
  DB_USER: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_MIN_32_BYTES"
