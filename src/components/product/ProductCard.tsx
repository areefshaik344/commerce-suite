/**
 * Canonical product card. Re-exports the existing shared implementation so
 * all callers (legacy + new catalog pages) share one component.
 */
export { ProductCard } from "@/components/shared/ProductCard";
export { ProductCard as default } from "@/components/shared/ProductCard";