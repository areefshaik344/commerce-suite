package com.commercesuite.analytics.domain;

/** Functional grouping for analytics events. Mirrors V016 enum. */
public enum AnalyticsCategory {
    CUSTOMER, VENDOR, CATALOG, INVENTORY, CHECKOUT,
    ORDER, PAYMENT, REFUND, PAYOUT, SYSTEM
}